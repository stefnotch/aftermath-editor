{
  "name": "mathml-editor",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --open",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "deploy": "vite build --base=/mathml-editor/ && node utils/publish.js"
  },
  "dependencies": {
    "vue": "^3.2.25"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^2.0.1",
    "gh-pages": "^3.2.3",
    "typescript": "^4.5.4",
    "vite": "^2.7.10",
    "vue-tsc": "^0.30.1"
  }
}
